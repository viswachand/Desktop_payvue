import { useTheme as useMuiTheme } from "@mui/material/styles";

/**
 * Reusable theme hook wrapper for consistent imports
 */
const useTheme = () => {
  return useMuiTheme();
};

export default useTheme;
